{
  "name": "tacedge-dashboard",
  "version": "1.0.0",
  "description": "Tactical Operations Dashboard for TacEdge Gateway",
  "scripts": {
    "start": "npx serve -s src -l 8080",
    "dev": "npx live-server src --port=8080"
  },
  "dependencies": {},
  "devDependencies": {
    "serve": "^14.2.0",
    "live-server": "^1.2.2"
  }
}

